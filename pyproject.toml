[project]
name = "serotonin-telegram-bot"
version = "1.0.0"
description = "Telegram football statistics bot powered by football-data.org"
authors = [{name = "Serotonin"}]
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot>=22.5",
    "httpx>=0.27",
    "rapidfuzz>=3.0",
]

[project.optional-dependencies]
test = []
